{
  "name": "ht-marineservice",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "install:all": "npm install",
    "start": "npm run start --workspace=backend",
    "deploy": "git push origin main && VITE_API_URL=https://ht-marineservice-backend-production.up.railway.app vercel build --prod --yes && vercel deploy --prebuilt --prod --yes"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
